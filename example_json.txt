Postman

curl -X POST http://localhost:8080/replicate \

  -H "Content-Type: application/json" \
  -d '{
    "PG_USER": "postgres",
    "PG_PASSWORD": "postgres",
    "PG_HOST": "localhost",
    "PG_PORT": "5432",
    "REPLICATION_CONFIGS": "dataset_id|nombre_basedatos"
}'
